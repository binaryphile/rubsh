#!/usr/bin/env bash

RUBSH_LIB=$(dirname "$(readlink -f "$BASH_SOURCE")")
export RUBSH_PATH+=${RUBSH_PATH:+:}$RUBSH_LIB:.
source "$RUBSH_LIB"/require.rubsh
unset -v RUBSH_LIB

$(require inits)
rubsh_init

$(require utils)

$(return_if_sourced)

bash --norc
